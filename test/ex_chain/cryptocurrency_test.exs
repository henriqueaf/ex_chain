defmodule ExChain.CryptocurrencyTest do
  use ExUnit.Case, async: true
  alias ExChain.Cryptocurrency
  doctest Cryptocurrency

  describe "ExChain.Cryptocurrency" do
    # setup(context) do
    #   {:ok, Map.put(context, :blockchain, Blockchain.new(0))}
    # end
  end
end
